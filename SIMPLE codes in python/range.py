#start from 1
bin = list(range(21))
#start from a specified number
bind = list(range(9, 21))
#start from a specified number and add to target
binder = list(range(3, 21, 3))

print(bin)
print(bind)
print(binder)